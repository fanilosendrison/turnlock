# Historical Bun source baseline

The byte-for-byte baseline remains available in Git at:

```text
2019a151f89fa8c5564e797dd006875286d46d77
```

This directory is documentation-only and excluded from runtime policy scans. Retrieve any historical file with `git show <baseline>:<path>`; do not copy it back into active packages or runners.

The immutable protocol fixture hashes are recorded in `test-parity.json`.
