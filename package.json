{
  "name": "turnlock",
  "version": "0.9.1",
  "description": "Durable finite-state-machine runtime for TypeScript. Snapshot-authoritative state, in-band stdout protocol, POSIX-grade I/O discipline. Host-agnostic: suspends by terminating its process and resumes from disk.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "bun test",
    "test:node-package": "npm run build && tsc -p tests/package/tsconfig.consumer.json && node --test tests/package/node-esm-distribution.test.mjs",
    "lint": "biome check src/ tests/",
    "prepack": "npm run test:node-package",
    "lint:fix": "biome check --write src/ tests/",
    "typecheck": "tsc --noEmit",
    "changelog": "git-cliff --output CHANGELOG.md"
  },
  "dependencies": {
    "zod": "^3.22.0",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@types/bun": "^1.1.0",
    "fast-check": "^3.23.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "UNLICENSED"
}
